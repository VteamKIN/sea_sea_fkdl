# 项目代码审查报告

> 审查范围：`code/` 与 `user/` 目录全部模块
> 审查时间：2026-04-26
> 按严重程度归类，每条问题包含：位置、现象、影响、修复建议

---

## ? Critical（建议尽快修复）

### 1. `encoder_conversion_l/r` 整数溢出风险

**位置**：`code/encoder.c:225-230`

```c
encoder_conversion_l = encoder_data_l * 38;
L_PID.ActualSpeed   = _wangyi_(encoder_conversion_l);

encoder_data_r       = -encoder_get_count(TIM4_ENCODER);
encoder_clear_count(TIM4_ENCODER);
encoder_conversion_r = encoder_data_r * 38;
```

**现象**：`encoder_data_l/r` 是 `volatile int16`（±32767），乘 38 后再赋给 `encoder_conversion_l/r`（也是 int16）。当 `encoder_data > 862` 时溢出（862 × 38 > 32767）。

**影响**：电机失控冲到极限编码器值时速度反馈错误。

**修复**：把 `encoder_conversion_l/r` 改为 `int32`，`encoder.h` 中 extern 同步改。

---

### 2. `Vofa_oscilloscope_receive` 多个 bug（当前死代码）

**位置**：`code/wireless_customer.c:94-101`

```c
for(int8 i=0;i<parser_index;i++)
{
    if(parser_buffer[i]==0xFF && parser_buffer[i+1]==0xFF)
    {
        Receive_data.byte[0]=parser_buffer[0];
        Receive_data.byte[1]=parser_buffer[1];
        Receive_data.byte[2]=parser_buffer[2];
        Receive_data.byte[3]=parser_buffer[3];
```

**三个并存问题**：

| # | 问题 | 后果 |
|---|---|---|
| a | `int8 i`（signed char）做循环计数器，`parser_index` 是 uint8 | buffer 长度 > 127 时因有符号溢出无限循环 |
| b | `parser_buffer[i+1]` 越界 | i = parser_index - 1 时访问 `parser_buffer[parser_index]` |
| c | 取数据用 `parser_buffer[0..3]` 而非 `[i..i+3]` | 永远只读前 4 字节，匹配位置无意义 |

**影响**：当前未在主流程调用，是隐藏地雷。未来启用 VOFA 接收时会立刻爆炸。

**修复**：
- `int8 i` → `int i` 或 `uint8 i`
- 循环条件 `i + 1 < parser_index`
- 数据下标 `parser_buffer[i+0..i+3]`

---

### 3. `last_error` 内存段不一致 + 重置缺失

**位置**：`code/control.c:86-101` 与 `code/control.c:457-468`

```c
#pragma section all restore   // line 86

// ... 函数注释 ...

int16 last_error = 0;          // line 101 — 不在 cpu0_dsram 段！
```

```c
void control_pid_reset(void)
{
    P = 0;
    I = 0;
    D = 0;
    error_image = 0;
    error_image_sum = 0;
    last_error_image = 0;
    output = 0;
    last_output = 0;
    // 漏了 last_error = 0;
}
```

**两个并存问题**：

- **段问题**：`last_error` 在 `#pragma section all restore` 之后定义，**不在 cpu0_dsram 段**，跨核访问慢。其他 error 变量都在段内。
- **重置缺失**：`control_pid_reset()` 清零了所有 error，但漏了 `last_error`。弯道完成调用 reset 后，下一帧 `error_image = 0.9*new + 0.1*last_error` 仍带历史误差。

**修复**：把 `int16 last_error = 0;` 移到 cpu0_dsram 段内（line 21-86 之间），并在 `control_pid_reset()` 末尾追加 `last_error = 0;`。

---

### 4. ISR 与 CPU1 数据竞争（无同步）

**位置**：`user/isr.c:44-54` + `code/control.c:438-447`

```c
IFX_INTERRUPT(cc60_pit_ch0_isr, 0, CCU6_0_CH0_ISR_PRIORITY)
{
    interrupt_global_enable(0);
    pit_clear_flag(CCU60_CH0);

    encoder_read();
    control_process();   // 内部调用 calc_error_image()
}
```

**现象**：
- ISR 在 CPU0，每 2ms 调用 `control_process` → `calc_error_image`
- `calc_error_image` 读 `left_edge[]/right_edge[]/left_edge_count` 等数组，由 CPU1 在 `img_process()` 中异步更新
- **没有同步机制**：可能读到 CPU1 写入边线数组的中间状态（部分新部分旧）

**影响**：偶发偏差计算失真。当前用 `cpu1_img_ready_flag` 同步显示，但 ISR 不检查此标志。

**修复（轻量）**：
- 方案 A：在 `control_process` 开头检查 `cpu1_img_ready_flag`，未就绪时复用上一帧 `error_image`
- 方案 B：CPU1 写边线时短暂禁用 CPU0 PIT 中断
- 方案 C（最简洁）：维护"图像帧序号"，control_process 仅在帧序号变化时重算 error_image

---

## ? High（潜在 Bug 与设计问题）

### 5. `error_image_sum` 死变量

**位置**：`code/control.c:69`、`control.h:41`、`control.c:464`

```c
vint16 error_image_sum = 0;    // 定义
extern vint16 error_image_sum; // extern 声明
error_image_sum = 0;           // reset 中清零
```

**现象**：定义、extern、reset 三处都有，但全局未被读写。

**修复**：删除 control.c:69、control.h:41、control.c:464 中的对应行。

---

### 6. `seekfree.c` 用 0 作为"无效"标记

**位置**：`code/seekfree.c:54-58`

```c
for (int y = 0; y < WARP_IMAGE_H; y++)
{
    if (left_x[y] > 0 && right_x[y] > 0)
        center_x[y] = (uint8)((left_x[y] + right_x[y]) / 2);
}
```

**现象**：`x=0` 是有效坐标（图像最左列），但被当作无效。当左边线触底贴边时该行的中线点会被错误丢弃。

**修复**：用 255 或专用标记初始化 `left_x/right_x`，或加并行 `valid` 数组。

---

### 7. `imu.c` 转弯函数死循环阻塞（潜在地雷）

**位置**：`code/imu.c:236-265, 267-293`（共 4 个函数）

```c
void turn_left_90(void)
{
    angle_turn_relative(90.0f);
    while (!angle_is_reached(ranges_of_angle_error))
    {
        // 角度环在中断中自动运行
    }
    angle_loop_enable_set(0);
}
```

**现象**：四个函数 `turn_left_90 / turn_right_90 / turn_left_absolute_90 / turn_right_absolute_90` 都用 `while (!angle_is_reached)` 阻塞。当前 IMU 未启用（`my_imu_init` 在 `init.c:29` 被注释），`yaw_angle` 永远 0，**这些函数一旦被调用即永久死循环卡死系统**。

**修复**：未来若启用前必须加超时；当前未调用可暂不动，建议添加 `// DEPRECATED — 未启用 IMU，禁止调用` 注释提示。

---

### 8. `image_send_seekffree` 设计问题

**位置**：`code/camera.c:19, 27-163`

```c
IFX_ALIGN(4) uint8 image_copy[MT9V03X_H][MT9V03X_W];   // 20 KB SRAM

void image_send_seekffree(void)
{
    seekfree_assistant_interface_init(SEEKFREE_ASSISTANT_DEBUG_UART);
    // ...
    while(1)
    {
        if(mt9v03x_init())                  // 重新初始化摄像头
            gpio_toggle_level(LED1);
        else
            break;
        system_delay_ms(500);
    }
    // ...
}
```

**三个并存问题**：

- `image_copy[128][160]` 占用 **20 KB SRAM**，但函数从不调用（cpu0_main.c 中已注释）
- 函数内含 `mt9v03x_init()` 重新初始化（赛车运行中调用会卡死摄像头）
- 函数内含 `while(1)` 阻塞循环

**修复**：删除整个 `image_send_seekffree()` 函数和 `image_copy` 数组，节省 20 KB SRAM；或加 `#if 0` 暂时屏蔽。

---

### 9. `detect_junction_type` 防抖 off-by-one

**位置**：`code/img_process.c:808-832`

```c
if (raw_junction == last_raw_junction)
{
    junction_stable_count++;
    if (junction_stable_count >= JUNCTION_STABLE_THRESHOLD)
    {
        current_junction = raw_junction;
        junction_detected = (raw_junction != JUNCTION_NONE) ? 1 : 0;
        junction_hold_count = JUNCTION_HOLD_FRAMES;     // 设为 5
        hold_junction_type = raw_junction;
    }
}
// ...

// 紧接着
if (junction_hold_count > 0)
{
    junction_hold_count--;                              // 立即减为 4
    current_junction = hold_junction_type;
    junction_detected = (hold_junction_type != JUNCTION_NONE) ? 1 : 0;
}
```

**现象**：防抖刚确认那一帧把 `hold_count` 设为 5，紧接着保持逻辑立刻减 1，实际保持只有 4 帧。

**影响**：路口保持帧数比预期少 1，影响小。

**修复**：把保持逻辑放进 `else` 分支，或先减后用。可选修复。

---

## ? Medium（代码质量）

### 10. `calc_error_image` 栈数组浪费

**位置**：`code/control.c:121-127`

```c
int16 left_min[32];      // 实际只用 y_span = 10
int16 right_max[32];
for (int i = 0; i < y_span; i++)
{
    left_min[i] = 32767;
    right_max[i] = -32768;
}
```

**现象**：`y_span = y_end - y_start = 75 - 65 = 10`，但数组分配 32。多浪费 88 字节栈空间。函数在 ISR 中调用，整体栈用量 ~870 字节，目前还能接受。

**修复**：改为 `int16 left_min[10]; int16 right_max[10];` 或定义 `#define ERR_Y_SPAN 10`。

---

### 11. menu 模块整体未被调用（死代码）

**位置**：整个 `code/menu.c` 与 `code/menu.h`

`menu_init()` / `menu_process()` 在源码中存在，但 `cpu0_main.c` 主循环不调用。`Debug/testest.map` 显示 `handle_pid_key` 标记 `unreferenced`。

**修复**：
- 选项 A：在 `cpu0_main.c` 主循环加入 `menu_process()`，恢复菜单功能
- 选项 B：删除 `menu.c/h`，节省代码空间

---

### 12. CPU1 处理被显示节奏限制

**位置**：`user/cpu1_main.c:71-81`

```c
if(mt9v03x_finish_flag && (0 == cpu1_img_ready_flag))
{
    img_process();
    __dsync();
    cpu1_img_ready_flag = 1;
    mt9v03x_finish_flag = 0;
}
```

**现象**：CPU1 必须等 CPU0 把 `cpu1_img_ready_flag` 清零才能处理下一帧。如果 CPU0 显示卡住（TFT IO ~ms），图像处理速度被限速。

**修复（可选）**：CPU1 处理完不等待，直接消费下一帧；CPU0 自由读最新结果（双缓冲或简单忽略 ready_flag）。当前 50Hz 摄像头帧率下不是瓶颈，可暂不动。

---

## ? Low（轻度优化建议）

### 13. cpu0_main.c 大量注释代码

**位置**：`user/cpu0_main.c:26-115`

约 60 行注释代码（line 26-115 大半被注释）。影响阅读，建议清理或归档到调试备忘文档。

---

### 14. `WarpPerspective` 每帧 memset

**位置**：`code/img_process.c:996-1012`

```c
void WarpPerspective(void)
{
    uint8 *src = &mt9v03x_image[0][0];
    uint8 *dst = &warp_image[0][0];
    memset(dst, 0, WARP_IMAGE_H * WARP_IMAGE_W);   // 每帧 ~10μs
    // ...
}
```

**现象**：每帧 memset 9920 字节（~10μs）。LUT 中 `WARP_LUT_INVALID` 位置永远不被覆盖，初始化时清零一次后续无需 memset。

**修复**：删除 `memset`。`warp_image` 在 `#pragma section` 块内全局变量默认 0 初始化，可省。

---

### 15. `camera.c` 死代码大量边界数组

**位置**：`code/camera.c:12-16`

```c
uint8 xy_x1_boundary[BOUNDARY_NUM], xy_x2_boundary[BOUNDARY_NUM], xy_x3_boundary[BOUNDARY_NUM];
uint8 xy_y1_boundary[BOUNDARY_NUM], xy_y2_boundary[BOUNDARY_NUM], xy_y3_boundary[BOUNDARY_NUM];
uint8 x1_boundary[MT9V03X_H], x2_boundary[MT9V03X_H], x3_boundary[MT9V03X_H];
uint8 y1_boundary[MT9V03X_W], y2_boundary[MT9V03X_W], y3_boundary[MT9V03X_W];
```

只在未调用的 `image_send_seekffree()` 内用，共占 ~1.5 KB SRAM。

**修复**：删除整套未用边界配置（与 #8 一并处理）。

---

## ? 推荐优先修复清单

| 优先级 | 问题 | 修复成本 | 风险/收益 |
|---|---|---|---|
| ??? | #3 `last_error` 段+重置 | 极低（2 行） | 弯道转直道残留误差 |
| ??? | #1 `encoder_conversion` 溢出 | 极低（改类型） | 高速电机控制失真 |
| ?? | #5 删除 `error_image_sum` | 极低 | 死代码清理 |
| ?? | #8 删除 `image_send_seekffree`+`image_copy` | 极低 | 释放 20 KB SRAM |
| ?? | #2 `Vofa_oscilloscope_receive` 修复或删除 | 低 | 避免未来启用时崩 |
| ? | #4 ISR 数据竞争 | 中 | 改善偏差计算稳定性 |
| ? | #15 删除 camera.c 死代码 | 低 | 释放 ~1.5 KB SRAM |
| ? | #11 决策 menu 模块去留 | 低 | 代码清理 |
| ? | #6 `seekfree.c` 无效标记 | 低 | 修正中线在边缘 |
| ? | #9 防抖 off-by-one | 极低 | 路口保持帧数准 |

---

## ? 全局指标

- **总死代码（建议删除）**：~22 KB SRAM 释放潜力（image_copy + camera.c 边界数组 + image_send_seekffree 函数）
- **Critical 级 bug**：4 个（其中 2 个会影响实际运行：#1 编码器溢出、#3 last_error 残留）
- **High 级问题**：5 个
- **Medium/Low 级优化**：6 个

## ? 关联文档

- `待实施的改进.md` — 已有的图像处理改进备忘
- `误差计算优化记录.md` — `calc_error_image` 三轮优化（y 加权 + 单边降级 + 众数邻域重心）
- `代码审查报告.md` — 本文档（最新审查结果）
