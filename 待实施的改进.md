# 待实施的改进

## 1. ~~左右手跟踪寻线修复~~ ?已完成

已改回左手跟左边线、右手跟右边线，起点保持在边界位置。

---

## 2. ~~路口检测改进：路宽范围限制 + 弯道相连判断~~ ?已完成

### 2a. 路宽范围限制

**问题**：原代码只有上限没有下限，弯道水平段可能超出上限被排除，但噪声/元器件也可能被误判为有路。

**修复**：四边判断统一为范围检查 `[road_pixel_min, road_pixel_max)`，下边只检查下限（底部正常行驶时路宽可能较宽）。

```c
// 路宽范围定义
int road_pixel_min = ROAD_PIXEL_THRESHOLD;     // 下限 = 5像素
int road_pixel_max = (int)(road_width_avg * 2); // 上限 = 平均宽度*2
if (road_pixel_max < ROAD_PIXEL_THRESHOLD * 2)
    road_pixel_max = ROAD_PIXEL_THRESHOLD * 2;

// 上边、左、右：范围检查
if (max_consecutive >= road_pixel_min && max_consecutive < road_pixel_max)

// 下边：只检查下限
if (max_consecutive >= road_pixel_min)
```

### 2b. 道路段起止坐标记录

扫描每边时记录最大连续道路段的起止坐标，用于弯道相连判断：

```c
int top_road_start = -1, top_road_end = -1;
int bottom_road_start = -1, bottom_road_end = -1;
int left_road_start = -1, left_road_end = -1;
int right_road_start = -1, right_road_end = -1;
// 扫描时同时更新这些变量
```

### 2c. 弯道相连判断

**问题**：弯道刚进入检测框时被误判为T字路口；直道偏移时一条道路同时被两个方向的检测框检测到而误判弯道类型。

**原理**：同一道路在两个边界的投影不应视为两个方向都有路。当角点处两边的道路段相连时，说明是弯道延伸而非路口分支。

```c
// 上+左相连：左弯道的道路从左上角弯入
if (road_top && road_left)
{
    if (top_road_start <= start_point_x + 5 && left_road_start <= start_point_y + 5)
    {
        road_top = 0;
        road_left = 0;       // 是弯道，不是路口
    }
}

// 上+右相连：右弯道的道路从右上角弯入
if (road_top && road_right)
{
    if (top_road_end >= end_point_x - 5 && right_road_start <= start_point_y + 5)
    {
        road_top = 0;
        road_right = 0;
    }
}

// 下+左相连：直道偏左时，只清除侧边标志（保留road_bottom）
if (road_bottom && road_left)
{
    if (bottom_road_start <= start_point_x + 5 && left_road_end >= end_point_y - 5)
    {
        road_left = 0;        // 只清侧边，不清底边（road_bottom是路口判断前提）
    }
}

// 下+右相连：直道偏右时
if (road_bottom && road_right)
{
    if (bottom_road_end >= end_point_x - 5 && right_road_end >= end_point_y - 5)
    {
        road_right = 0;
    }
}
```

**文件**：`img_process.c` `detect_junction_type()` 函数

---

## 3. choose数组简化

删除 `-99` 值，只用 0（循左线）、1（循右线）、-1（停止）三个值。

| 文件 | 改动 |
|------|------|
| `img_process.c:56` | 初始值 `-99` → `-1`，注释去掉 `-99` |
| `img_process.h:136` | 注释去掉 `-99` |
| `control.c:216` | `== -99 \|\|` 删除 |
| `control.c:229` | `!= -99 &&` 删除 |

---

## 4. 元器件干扰防御（已实施 ?）

### 问题分类

| 元器件 | 症状 | 根因 | 解决层 |
|--------|------|------|--------|
| **电容** | 边线包住电容近车侧，远侧无线，误差保持上一帧被污染值 | 跟踪器被横线吸引后折返，y出现回头 | 第一层：边线回折截断 |
| **三极管/MOS管** | 中线严重偏移，车身偏向一侧 | 横向白线拉偏单侧边线，均值计算受异常点影响 | 第二层：中位数替代均值 |
| **电容**（补充） | 单侧丢线时强行计算中线偏移 | 两侧点数不对称，中线计算不可靠 | 第三层：边线质量检测 |

### 第一层：边线回折截断 ?（解决电容丢线的根本原因）

**位置**：`img_process.c` `zi_shi_ying_warp()` 跟踪完成后

**原理**：正常边线从底部向上走，y单调递减。电容横线导致跟踪器折返向下走，y连续递增。检测连续y递增的点数，超过阈值则视为回折，截断后续点。

**为什么不用全局y_min比较**：直角弯+车身偏移时，边线可能先到达很高位置（y很小）再弯回来（y增大），用全局y_min会误截断弯道后的正常边线。

**连续递增 vs 全局比较的区别**：
```
直角弯+偏移： y = [79,78,77,76,75,74,73,72,73,74,75]  ← 弯角后y增大，但非连续递增（有平段）
                                                  ↑ 只有1-2个点连续递增，不触发截断

电容回折：    y = [79,78,77,76,77,78,79,80,81,82]     ← 折返后y持续递增
                                           ↑ 连续5+个点递增，触发截断
```

**实现**：
```c
int fold_count = 0;
for (int i = 1; i < edge_count; i++)
{
    if (edge[i][1] > edge[i-1][1])   // y递增
        fold_count++;
    else
        fold_count = 0;              // y不递增则重置
    if (fold_count >= EDGE_FOLD_THRESHOLD)
    {
        edge_count = i - fold_count + 1;  // 截断到递增开始处
        break;
    }
}
```

**为什么不影响弯道**：
- 直角弯边线在弯角处y基本不变或微增1像素，不会连续4个点以上递增
- 只有被横线吸引折返向下走的情况，y才会持续递增5+个点
- 车身偏移导致弯道后y增大时，中间有平段或微减段打断连续递增计数

**宏定义**：`EDGE_FOLD_THRESHOLD = 4`（`img_process.h`）

### 第二层：中位数替代均值 ?（解决三极管/MOS管偏移）

**位置**：`control.c` `calc_error_image()` 全部分支

**原理**：均值受所有点影响，个别异常点就能大幅偏移结果。中位数取排序后的中间值，少量异常点不影响结果。

**对弯道无害**：中位数比均值更稳定，正常数据和异常数据都是如此。

### 第三层：边线质量检测 ?（解决电容丢线的补充防御）

**位置**：`control.c` `calc_error_image()` 直道分支

**原理**：如果第一层（回折截断）后仍然单侧丢线，直道模式下要求两侧都有足够采样点（至少各2个），否则不更新误差。

### 角点检测清理 ?（已删除）

已删除 `detect_corners()`、`detect_corner_type()`、`corner_road_type` 变量及相关枚举和宏。
主流程精简：逆透视 → 边线检测(含回折截断) → 路口检测 → 方向判断 → 丢线检测

---

## 5. T字弯误判原因分析（仅分析不修复）

**现象**：T字形道路偶尔识别为 JUNCTION_LEFT(1) 或 JUNCTION_RIGHT(2)，而非正确的 JUNCTION_T

**已排除的原因**：
- ~~元器件噪声~~ — 测试环境无干扰
- ~~车身偏移~~ — 与错误发生无关

**最可能原因**：边缘效应
T字路口某一侧臂端部恰好在检测框侧边界附近，帧间微小变化（图像噪声、车身微动）导致该侧的连续白色像素数在阈值附近波动——有时>=5被判为有路，有时<5被判为无路。这导致T字路口偶尔只有单侧被检测到，被识别为左直角或右直角。

**缓解措施**（已在任务2的路宽范围限制中实现）：
- 加入路宽下限提高判断稳定性
- 如果后续仍出现，可考虑将 `ROAD_PIXEL_THRESHOLD` 从5提高到7-8
