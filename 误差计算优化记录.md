# `calc_error_image` 误差计算优化记录

> 文件位置：`code/control.c`
> 优化日期：2026-04-26 ~ 2026-04-27
> 涉及函数：`calc_error_image()`、新增 `get_hist_mode_center()`

---

## 📍 背景

赛车窄赛道（约 7 像素宽），赛道上含三极管等元器件作为标识。原 `calc_error_image` 用直方图中位数计算虚拟中线偏差，在以下场景下精度不足：

1. **直角弯（90°）转折点**：边线在采样区出现 L 形（垂直段 + 水平段），中位数被远景水平段拉偏
2. **直道一侧边线缺失**：一边被遮挡时直接衰减误差为 0，浪费另一边的有效信息
3. **三极管干扰**：边线跟踪器绕三极管走一圈，导致边线 x 在某段 y 内出现"凸起"，中位数被加权放大的离群点拉偏

---

## 🔧 三轮优化（按顺序）

### 优化 1：y 加权直方图

**问题**：直方图 `hist[x]` 累加时所有点权重相同，远景水平段（y 小）和近景垂直段（y 大）权重一样，中位数被远景拉偏。

**方案**：每个边线点累加权重 `weight = y - y_start + 1`，权重范围 1~10，近景占主导。

| y 坐标 | 权重 | 物理含义 |
|---|---|---|
| 65（远） | 1 | 远景，弯道转折点 |
| 70（中） | 6 | 中景 |
| 74（近） | 10 | 近景，跟车点 |

**实现**：
- 新增 `weight_sum / left_weight_sum / right_weight_sum`（加权和，传 `get_hist_median` 的 total）
- 保留 `left_raw_count / right_raw_count`（原始点数，用于 `>= 2` / `>= 5` 阈值判定）
- 三个 case（straight / left / right）均统一加权

```c
int w = y - y_start + 1;
hist[x]      = (uint16)(hist[x]      + w);
hist_left[x] = (uint16)(hist_left[x] + w);
weight_sum      += w;
left_weight_sum += w;
left_raw_count++;
```

---

### 优化 2：单边降级（case straight）

**问题**：直道场景下若一边边线点 < 2（如障碍物遮挡、弯道前的边线截断），原代码直接 `error_image *= 7/10` 衰减返回，**完全浪费充足那一边的信息**。

**方案**：当一边充足、另一边不足时，用充足一边的主峰 + `road_width_avg` 估算虚拟中线：

```
左边线缺失 → 虚拟中线 = right_peak - road_width_avg / 2
右边线缺失 → 虚拟中线 = left_peak  + road_width_avg / 2
```

`road_width_avg = 0` 时 fallback 到 7（窄赛道宽度先验）。

**实现**：
- 新增 `virtual_center_override` 标志（>=0 表示走单边降级路径）
- 单边降级路径直接计算覆写值，跳过 hist median

---

### 优化 3：众数邻域重心（抗三极管离群点）

**问题**：边线跟踪器（`findline_lefthand_adaptive` / `findline_righthand_adaptive`）遇到赛道内三极管会绕其外缘走一圈，导致边线点在某段 y 内出现 x 凸起。`EDGE_FOLD_THRESHOLD=4` 只能截断"完全反向回折"，截不掉短暂横向偏移。

中位数对"少数离群点"鲁棒，但对"中等比例的偏移点"不鲁棒。叠加 y 加权后，**近景三极管离群点权重被放大**，反而让中位数更偏。

**方案**：单边场景改用"直方图主峰邻域加权重心"（mode 替代 median）：
- **主流边线**点集中在某个 x（如 x=30）→ `hist[30]` 单值最高
- **三极管离群点**散布在多个 x（35~40）→ 单值不高，分散
- 直方图最大峰 → 主流边线
- 在 `peak ± 3` 邻域内加权求重心 → 平滑离群点

```c
#define HIST_PEAK_RADIUS  3
static int16 get_hist_mode_center(const uint16 *hist, int total)
{
    if (total <= 0) return (int16)(WARP_IMAGE_W / 2);

    // 1. 扫描最大值位置
    int max_val = 0;
    int peak_x = WARP_IMAGE_W / 2;
    for (int x = 0; x < WARP_IMAGE_W; x++)
    {
        if (hist[x] > max_val) { max_val = hist[x]; peak_x = x; }
    }

    // 2. 主峰邻域加权重心
    int x_lo = peak_x - HIST_PEAK_RADIUS; if (x_lo < 0) x_lo = 0;
    int x_hi = peak_x + HIST_PEAK_RADIUS; if (x_hi >= WARP_IMAGE_W) x_hi = WARP_IMAGE_W - 1;
    int sum_xw = 0, sum_w = 0;
    for (int x = x_lo; x <= x_hi; x++)
    {
        sum_xw += x * hist[x];
        sum_w  += hist[x];
    }
    if (sum_w > 0) return (int16)(sum_xw / sum_w);
    return (int16)peak_x;
}
```

**关键决策**：直道双边场景**不用 mode**，因为 `hist` 是双峰（左右边线），mode 会跑到一边。

---

## 🎯 算法选择策略

| 场景 | virtual_center 算法 | 原因 |
|---|---|---|
| `case straight` 双边充足 | **median**（hist） | 双峰中位数 = 中线 |
| `case straight` 单边降级 | **mode**（hist_左/右）+ 宽度推算 | 单峰 + 抗离群 |
| `case left` | **mode**（hist） | 单峰，循左边线 |
| `case right` | **mode**（hist） | 单峰，循右边线 |
| 双边都 < 2 点 | 衰减返回 | 数据严重不足 |
| 总点数 < 5 | 衰减返回 | 信息量不够 |
| 左右交叉 | 衰减返回 | 异常状态 |

---

## 📊 优化前后对比

### 直角弯转折点（左弯 + L 形边线）

主流垂直段 6 行 x≈30，远景水平段 4 行 x≈60：

| 算法 | 计算结果 | 偏差 |
|---|---|---|
| 原 median（无加权） | x ≈ 45（被水平段拉偏） | ❌ 偏 15 px |
| **y 加权 median** | x ≈ 32（近景占优） | ✓ 偏 2 px |

### 三极管干扰（左弯，主流 x=30 6 行 + 离群 x=38~40 4 行近景）

| 算法 | 计算结果 | 偏差 |
|---|---|---|
| y 加权 median | x ≈ 35（被加权离群拉偏） | ❌ 偏 5 px |
| **y 加权 mode** | x ≈ 30（主峰邻域 28~32 重心） | ✓ 准确 |

### 直道一侧遮挡（左边线缺失）

| 算法 | 输出 | 影响 |
|---|---|---|
| 原代码 | `error *= 0.7` 衰减为 0 | ❌ 车直行 |
| **单边降级** | 用 `right_peak - 7/2` 推算 | ✓ 仍能跟线 |

---

## 🔍 实施位置参照

| 改动 | 文件:行号 |
|---|---|
| `get_hist_median` 函数 | `code/control.c:37-50` |
| `get_hist_mode_center` 函数（新增） | `code/control.c:55-89` |
| `HIST_PEAK_RADIUS=3` 宏 | `code/control.c:55` |
| `calc_error_image` 函数 | `code/control.c:140-372` |
| y 加权累加（三个 case） | `code/control.c:178-294` |
| 单边降级路径 | `code/control.c:233-247` |
| `use_mode_for_main` 算法切换 | `code/control.c:174,269,292,313-316` |
| 栈数组缩容 [32]→[10] | `code/control.c:162-163` |

---

## ⚙️ 调参建议

实测后若仍有问题，可调整以下参数：

| 参数 | 默认值 | 调整方向 |
|---|---|---|
| `HIST_PEAK_RADIUS` | 3 | 三极管离群比例高 → 调小到 2（更激进拒绝） |
| 采样区 `y_start/y_end` | 65/75 | 入弯响应慢 → y_start 提前到 60 或 55 |
| `curve_offset` | ±3 | 内切策略激进度，赛道窄可减到 ±2 |
| 误差限幅 ±50 | 50 | 窄赛道收敛 → 减到 30 |
| 平滑滤波系数 0.9 | 0.9 | 抖动多 → 减到 0.7（更平滑但慢响应） |

---

## ✅ 验证清单

实机测试时用 VOFA 监控 `error_image` 与 `error_left/error_right`：

- [ ] **直道空赛道**：`error_image` 应在 ±5 内稳定
- [ ] **直道遮挡一边**：`error_image` 仍能跟线（不再衰减为 0）
- [ ] **直角弯入口**：`error_image` 平滑过渡，无明显跳变
- [ ] **直角弯转折点**：无 L 形边线导致的瞬间大偏差
- [ ] **三极管处**：`error_image` 应保持平稳（不出现 5~10 px 瞬间偏移）
- [ ] **PID 输出 `output`**：弯道下不饱和（不持续撞 ±OUTPUT_LIMIT）

---

## 🔗 关联文档

- `代码审查报告.md` — 全局代码审查
- `待实施的改进.md` — 早期改进备忘
