### 远端寻线策略（应对电容遮挡/断线）

- **目标**：底部被遮挡或断线时，仍能在更远处恢复车道，给当前误差提供可靠参考。

- **触发条件**：
  - 底部扫描宽度过窄（如 < 8 像素）或连续丢线计数超阈值（例如 >3 帧）。
  - 进入“远端寻线模式”，提升远端观测权重。

- **退出条件**：
  - 连续若干帧（3~5）底部宽度恢复正常且丢线计数清零，退出远端模式，权重回归。

- **核心流程**：
  1. **多层起点**：底部异常时，向上多取 1/3H、1/2H 水平带做扫描，找到最长连续白段作为临时起点候选。
  2. **远端窗口扫描**：在上部小窗做水平直方图/最长白段搜索，得到远端中心 `far_mid` 及左右边界 `far_left/right`。
  3. **左右分治外推**：
     - 若一侧可见、一侧缺失：用可见侧 + 远端中心线性外推缺失侧或底部中心。
     - 若双侧缺失：用 `far_mid` 直接作为临时底部目标，并降低控制权重/速度。
  4. **拟合与融合**：
     - 用底部（若有）+ 远端点做一阶拟合，得到底部目标点；
     - 按置信度融合：底部>远端，当进入远端模式时提升远端权重，恢复后衰减。
  5. **控制侧配合**：远端模式下可限速、限转向幅度，防止远端估计抖动带来大舵量。

- **参数建议（可按现场调节）**：
  - 底部最小宽度 `min_width`：8~12 像素。
  - 触发丢线帧数：3~5 帧。
  - 远端窗口高度：1/3H 与 1/2H 两层，窗口宽度全宽或分左右半幅。
  - 退出帧数：3~5 帧连续正常。
  - 远端权重衰减：退出模式后 3~5 帧线性衰减回正常。

- **实施提示（不改现有控制逻辑）**：
  - 仅在图像处理阶段实现“多层扫描 + 远端外推 + 权重融合”，向控制层输出平滑的底部目标误差即可。
  - 可在屏幕/串口标注远端模式状态，便于调试判定是否触发。
  - 若赛道黑白反差小，远端窗口阈值可用局部均值+clip，自适应到该高度的光照。